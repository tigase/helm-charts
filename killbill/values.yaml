
imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

killbill:
  version: "0.24"
  image:
    repository: killbill/killbill
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""

  enableAdminUser: false
  admin:
    password: ""

  plugins:
    persistentVolume:
      enabled: false
      storage: 1Gi
    
  resources: {}
  affinity: {}
  nodeSelector: {}

  service:
    annotations: {}
    type: LoadBalancer     # Since the service is exposed outside the cluster, this cannot be ClusterIP.
    port: 8080
    whitelistSourceRange: "141.148.186.226/32"  # Points to IP of Uptime Kuma IP; can be expanded to include
                                                # IP of other management servers from where we might want to
                                                # troubleshoot the inventory checker.

# Inventory checker sidecar attributes, with service/ingress port.
#
sidecar:
  enabled: true  # Enable or disable the sidecar container
  name: "inventory-checker"  # Sidecar container name
  image: "inventory-checker-sidecar"  # Sidecar image name
  tag: "latest"  # Sidecar image tag

  service:
    enabled: true  # Enable or disable the sidecar service
    port: 8080     # Port for the service
    type: ClusterIP  # LoadBalancer is not needed since Ingress controller can route
                     # external traffic to ClusterIP.
  ingressHost: "inventory-checker.tigase.net"  # FQDN for the inventory checker service


kaui:
  image:
    repository: killbill/kaui
    pullPolicy: IfNotPresent
    tag: "2.0.11"

  rootUser: "admin"

  service:
    annotations: {}
    type: ClusterIP
    port: 8080

database:
  host: mysql
  port: 3306
  killbill: "killbill"
  kaui: "kaui"
  existingSecret: ""
  loadSchema: false

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  hosts:
    - host: killbill
      paths:
        - path: /
          pathType: ImplementationSpecific
          serviceType: "killbill"
    - host: kaui
      paths:
        - path: /
          pathType: ImplementationSpecific
          serviceType: "kaui"
  ## Name of the SSL certificate provider to use from:
  ## - self-signed
  ## - letsencrypt
  sslProvider: "self-signed"
  certificateIssuer: ""
  tls: {}
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local
  
podAnnotations: {}

podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
# runAsUser: 1000
